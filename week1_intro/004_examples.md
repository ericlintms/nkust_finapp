# Week 1: 實務範例與實作

這週我們先在單純的 Python 環境裡練功，把之後會用到的財務邏輯函數給封裝起來。我們寫的練習檔案都會放在 `examples/` 資料夾底下。

## 我們的練習清單

### 0. 前置練習：Python 基礎熱身

- **對應檔案**: `examples/w100_python_basics.py`
- **練習目的**: 在進入財務公式前，先確認 `list`、`dict`、函式 (`def`) 這三種最重要的 Python 基礎工具我們都操作熟悉了。財務模型的每條公式，最終都會被包成函式，而一系列的現金流就是存在 `list` 裡面的——所以這三樣東西是我們整個課程的地基！
- **實作任務**: 試著自己寫一個 `def create_portfolio(name, amount, rate)` 函式，讓它回傳一個字典 `{'name': ..., 'fv_10yr': ...}`，把對應的 10 年終值也算進去。

### 1. 例題一：手刻一個淨現值計算機

- **對應檔案**: `examples/w101_npv_calculator.py`
- **練習目的**: 看看如果我們不偷懶用別人寫好的套件，光靠基礎的 `for` 迴圈跟 `**` (次方運算) 來把 NPV 公式做出來。
- **實作任務**: 我們可以進去程式碼裡，試著改改看 `cash_flows` 這個變數，還有調整 `discount_rate`，看最後算出來的結果有什麼變化。

### 2. 例題二：用外部強大套件來算 IRR

- **對應檔案**: `examples/w102_irr_calculator.py`
- **練習目的**: 學一下怎麼用 `pip install` 找別人寫好的專家套件來幫我們工作。因為算 IRR 需要用到逼近法，我們自己手寫會很痛苦而且效率可能不好，所以這裡就直接請出強大的 `numpy_financial`。
- **實作任務**: 我們自己試著丟一組現金流陣列（例如有正有負的 `[-100, 20, -10, 50, 60]`），交給程式去幫我們跑出它的 IRR。
